{
  "scripts": {
    "scrape": "npx cypress run",
    "postscrape": "git add . && git commit -m 'Update' && git push",
    "publish": "git add . && git commit -m 'Update' && git push"
  },
  "dependencies": {
    "cypress": "^15.8.2"
  }
}
